---
name: Browser Verifier
description: The integration layer for the Reinforcement Learning validation. Boots a headless Playwright instance and runs RLlib.
---

# Browser Verifier
Agent instructions:
1. Trigger this skill to unleash the Ray RLlib PPO model against the live DOM.
2. It requires `npm run dev` to be active.
3. The script will aggressively hunt for UI state violations and navigation bypasses.
